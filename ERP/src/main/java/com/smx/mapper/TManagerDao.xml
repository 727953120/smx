<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.smx.dao.TManagerDao">

    <select id="login" resultType="com.smx.model.TManager">
        select * from T_MANAGER WHERE M_NAME=#{mName} and M_PASS=#{mPass}
    </select>
</mapper>